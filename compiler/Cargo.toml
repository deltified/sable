[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
inkwell = { version = "0.9.0", default-features = false, features = ["llvm22-1"] }

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
